---
import Avatares from './ui/Avatares.astro';
import ProjectsCarousel from './ui/ProjectsCarousel.astro';
---

<section class="relative h-full flex flex-col items-center justify-center pt-20">
  <div
    class="absolute inset-0 -z-10 2xl:h-153 lg:h-147 w-full
      [background:radial-gradient(120%_100%_at_50%_10%,var(--color-primary)_42%,var(--color-secondary)_100%)] rounded-b-3xl"
  >
  </div>

  <div class="relative z-10 w-full max-w-5xl text-center">
    <!-- Badge -->
    <div
      class="inline-flex items-center bg-sixth px-3 py-1 rounded-full mb-4 lg:mb-8 2xl:mb-12 text-white font-normal"
    >
      <span class="text-sm text-gray-200">⚡Bienvenido</span>
    </div>

    <!-- Título -->
    <h1
      class="text-4xl text-balance lg:text-5xl xl:text-6xl font-medium tracking-tighter text-white leading-tight mb-1 lg:mb-4 2xl:mb-6"
    >
      Creamos las herramientas,
      <br />
      digitales que usas cada día
    </h1>
  </div>

  <!-- Descripción -->
  <p
    class="text-seventh text-sm text-pretty md:text-xl mb-10 max-w-3xl mx-auto"
  >
    Desarrollamos y mantenemos un ecosistema de aplicaciones web enfocadas en la
    música, el diseño y la productividad para miles de usuarios globales.
  </p>

  <!-- Botones -->
  <div
    class="flex flex-col sm:flex-row items-center justify-center gap-4 mb-12"
  >
    <a
    href="#proyectos"
      class="cursor-pointer bg-quaternary hover:bg-white hover:text-gray-800 text-white font-semibold px-8 py-3 rounded-full transition-colors ease-in-out duration-500"
    >
      Explorar Proyectos
  </a>
    <button
      class="cursor-pointer bg-tertiary text-white font-semibold px-8 py-3 rounded-full transition border border-gray-600 hover:border-gray-900 ease-in-out duration-500"
    >
      Blog
    </button>
  </div>

  <!-- Avatares -->
  <Avatares />
</section>

<ProjectsCarousel />
