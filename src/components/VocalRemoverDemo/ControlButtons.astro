---
/**
 * Componente de Botones de Control
 * Play/Pause, Stop, Reset
 */
---

<style>
  .btn-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.5rem;
    height: 2.5rem;
    border-radius: 0.5rem;
    cursor: pointer;
    transition: all 0.3s ease;
    border: none;
    font-size: 1rem;
  }

  .btn-play {
    background: linear-gradient(135deg, rgb(16, 185, 129), rgb(5, 150, 105));
    color: white;
    box-shadow: 0 4px 6px rgba(16, 185, 129, 0.3);
  }

  .btn-play:hover {
    transform: scale(1.1);
    box-shadow: 0 8px 12px rgba(16, 185, 129, 0.5);
  }

  .btn-secondary {
    background: rgb(55, 65, 81);
    color: white;
    transition: background 0.3s ease;
  }

  .btn-secondary:hover {
    background: rgb(75, 85, 99);
  }

  .controls {
    display: flex;
    gap: 1rem;
    align-items: center;
  }

  .icon {
    width: 1.5rem;
    height: 1.5rem;
  }
</style>

<div class="controls">
  <button id="playPauseBtn" class="btn-icon btn-play" title="Play/Pause">
    <svg id="playIcon" class="icon" fill="currentColor" viewBox="0 0 24 24">
      <path d="M8 5v14l11-7z"></path>
    </svg>
    <svg id="pauseIcon" class="icon hidden" fill="currentColor" viewBox="0 0 24 24">
      <rect x="6" y="5" width="4" height="14" rx="1"></rect>
      <rect x="14" y="5" width="4" height="14" rx="1"></rect>
    </svg>
  </button>

  <button id="stopBtn" class="btn-icon btn-secondary" title="Stop">
    <svg class="icon" fill="currentColor" viewBox="0 0 24 24">
      <rect x="6" y="6" width="12" height="12" rx="1"></rect>
    </svg>
  </button>

  <button id="resetBtn" class="btn-icon btn-secondary" title="Reset">
    <svg class="icon" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
      <path stroke-linecap="round" stroke-linejoin="round" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
    </svg>
  </button>
</div>
