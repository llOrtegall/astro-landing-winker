---
interface Props {
  id: string;
  className?: string;
}

const { id, className } = Astro.props;
---

<section id={id} class={`w-full max-w-7xl mx-auto px-4 sm:px-6 py-10 ${className ?? ''}`}
>
    <slot />
</section>